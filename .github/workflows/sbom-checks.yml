name: SBOM Branch Checks

on:
  pull_request:
    branches: ["main", "production", "acceptation", "test", "dev"]
    paths:
      - docs/sbom/**

jobs:
  CodeQL:
    name: CodeQL
    runs-on: ubuntu-latest
    steps:
      - run: echo "Skipped for SBOM-only PR"

  osv-scan:
    name: osv-scanner
    runs-on: ubuntu-latest
    steps:
      - run: echo "Skipped for SBOM-only PR"