name: CI — Build, Test & Security

on:
  push:
    branches: ["main", "production", "acceptation", "test", "dev"]
  pull_request:
    branches: ["main", "production", "acceptation", "test", "dev"]

permissions:
  contents: read
  security-events: write   # CodeQL / SARIF upload
  pull-requests: write     # Dependency Review comments

jobs:
  # ──────────────────────────────────────────────
  # 1. BUILD
  # ──────────────────────────────────────────────
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: "8"
          distribution: "temurin"
          cache: maven

      - uses: ./.github/actions/setup-openmrs-maven

      - name: Build
        run: mvn clean package --no-transfer-progress -DskipTests -P ci

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts
          path: "**/target/*.jar"
          retention-days: 90   # NEN-7510: beveiligingsrapporten langer bewaren

  # ──────────────────────────────────────────────
  # 2. TESTS
  # ──────────────────────────────────────────────
  test:
    name: Tests
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: "8"
          distribution: "temurin"
          cache: maven

      - uses: ./.github/actions/setup-openmrs-maven
 
      - name: Run tests & genereer coverage rapport
        run: |
          mvn verify jacoco:report \
            --no-transfer-progress \
            -pl api,api-1.9,api-1.10,api-2.0,api-2.2,api-tests \
            -am -P ci
 
      - name: Verzamel coverage rapporten
        if: always()
        run: |
          mkdir -p coverage-reports
          find . -path "*/target/site/jacoco" -type d | while read dir; do
            module=$(echo "$dir" | cut -d/ -f2)
            cp -r "$dir" "coverage-reports/$module"
          done

      - name: Upload coverage rapport
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage-reports/
          retention-days: 90
 
      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-reports
          path: "**/target/surefire-reports/**"
          retention-days: 90   # NEN-7510: beveiligingsrapporten langer bewaren

  # ──────────────────────────────────────────────
  # 3. CODEQL — SAST (Java 8 safe mode)
  # ──────────────────────────────────────────────
  codeql:
    name: CodeQL SAST
    runs-on: ubuntu-latest
    needs: build
    permissions:
      actions: read
      contents: read
      security-events: write
    strategy:
      fail-fast: false
      matrix:
        language: ["java"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: "8"
          distribution: "temurin"
          cache: maven

      - uses: ./.github/actions/setup-openmrs-maven

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: ${{ matrix.language }}

      - name: Build for CodeQL (without omod)
        run: |
          mvn clean package \
            --no-transfer-progress \
            -DskipTests \
            -pl api,api-1.9,api-1.10,api-2.0,api-2.2,api-tests \
            -am

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
        with:
          category: "/language:${{ matrix.language }}"
          upload: true
          output: codeql-results

      - name: Upload CodeQL SARIF
        uses: actions/upload-artifact@v4
        with:
          name: codeql-sarif
          path: codeql-results/
          retention-days: 90   # NEN-7510: beveiligingsrapporten langer bewaren
      
      - name: Upload compiled classes
        uses: actions/upload-artifact@v4
        with:
          name: compiled-classes
          path: |
            **/target/classes/**

  # ──────────────────────────────────────────────
  # 4. DEPENDENCY REVIEW (alleen op PR)
  # ──────────────────────────────────────────────
  dependency-review:
    name: Dependency Review
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v4
      - name: Dependency Review
        uses: actions/dependency-review-action@v4
        with:
          fail-on-severity: high
          deny-licenses: GPL-3.0, AGPL-3.0

  # ──────────────────────────────────────────────
  # 5. SBOM — anchore/sbom-action (geen Maven build nodig)
  # Genereert SBOM direct van de broncode / pom.xml
  # ──────────────────────────────────────────────
  sbom:
    name: Generate SBOM
    runs-on: ubuntu-latest
    needs: build
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4

      - name: Generate SBOM (no build dependency)
        uses: anchore/sbom-action@v0
        with:
          format: cyclonedx-json
          output-file: sbom.cyclonedx.json
          upload-artifact: false

      - name: Upload SBOM (anchore-action output)
        uses: actions/upload-artifact@v4
        with:
          name: sbom-cyclonedx-anchore
          path: sbom.cyclonedx.json
          retention-days: 90

      - name: Prepare SBOM file in repo
        if: github.event_name == 'push'
        run: |
          mkdir -p docs/sbom
          cp sbom.cyclonedx.json docs/sbom/sbom.cyclonedx.json

      - name: Generate App token for PR creation
        if: github.event_name == 'push'
        id: app-token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.SBOM_APP_ID }}
          private-key: ${{ secrets.SBOM_APP_PRIVATE_KEY }}

      - name: Create PR with updated SBOM
        if: github.event_name == 'push'
        id: cpr
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "chore: update SBOM [skip ci]"
          title: "chore: update SBOM"
          body: "Automatisch gegenereerde SBOM-update door de CI-pipeline."
          branch: chore/update-sbom
          delete-branch: true
          add-paths: docs/sbom/sbom.cyclonedx.json

      - name: Approve PR with App token
        if: github.event_name == 'push' && steps.cpr.outputs.pull-request-number
        run: gh pr review --approve "${{ steps.cpr.outputs.pull-request-number }}"
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}

      - name: Enable auto-merge
        if: github.event_name == 'push' && steps.cpr.outputs.pull-request-number
        run: gh pr merge --auto --squash "${{ steps.cpr.outputs.pull-request-number }}"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # ──────────────────────────────────────────────
  # 6. OSV SCAN
  # ──────────────────────────────────────────────
  osv-scan:
    name: OSV Scanner
    needs: sbom
    permissions:
      actions: read
      contents: read
      security-events: write
    uses: google/osv-scanner-action/.github/workflows/osv-scanner-reusable.yml@v2.3.8
    with:
      download-artifact: sbom-cyclonedx-anchore
      scan-args: |-
        --sbom=sbom.cyclonedx.json

  # ──────────────────────────────────────────────
  # 7. SONARQUBE SCAN
  # ──────────────────────────────────────────────
  sonar:
    name: SonarQube Scan
    runs-on: ubuntu-latest
    needs: test

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Java 8 (compile) and Java 17 (sonar)
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: |
            8
            17
          cache: maven

      - uses: ./.github/actions/setup-openmrs-maven

      - name: Download coverage reports
        uses: actions/download-artifact@v4
        with:
          name: coverage-report
          path: coverage-reports/

      - name: Build & verify with Java 8
        env:
          JAVA_HOME: ${{ env.JAVA_HOME_8_X64 }}
        run: |
          mvn verify \
            --no-transfer-progress \
            -pl api,api-1.9,api-1.10,api-2.0,api-2.2,api-tests \
            -am -P ci \
            -DskipTests

      - name: SonarQube Scan with Java 17
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          JAVA_HOME: ${{ env.JAVA_HOME_17_X64 }}
        run: |
          mvn sonar:sonar \
            --no-transfer-progress \
            -pl api,api-1.9,api-1.10,api-2.0,api-2.2,api-tests \
            -am -P ci \
            -Dsonar.projectKey=${{ secrets.PROJECT_KEY }} \
            -Dsonar.organization=${{ secrets.ORGANIZATION_KEY }} \
            -Dsonar.host.url=${{ secrets.SONAR_HOST_URL }} \
            -Dsonar.java.source=8 \
            -Dsonar.java.binaries=target/classes \
            -Dsonar.coverage.jacoco.xmlReportPaths=**/target/site/jacoco/jacoco.xml